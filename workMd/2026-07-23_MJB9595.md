# [작업 로그] 2026-07-23 — 김재아 (MJB9595)

> 작성자: 김재아 (GitHub: `MJB9595`)
> 날짜: 2026-07-23

---

## 오늘 작업

### 1. 직원 셀프 비밀번호 변경 UI 및 기능 추가
- 직원 본인이 직접 비밀번호를 변경할 수 있도록 프론트엔드 UI(A1)를 구현하고 연동.

### 2. Mock 화면 백엔드 연동 및 월근태 집계 API
- 기존 프론트엔드에 하드코딩(Mock)되어 있던 5종의 화면을 실제 백엔드 API와 연동 (#4).
- 월근태 집계 API 구현 및 적용.
- 화면 내 라벨 정리 작업 (#11).

### 3. 백엔드 견고성 및 보안 개선
- 셀프 비밀번호 변경 로직의 오류 처리 강화.
- 404 에러 처리 개선 및 낙관적 락(Optimistic Lock) 적용을 통한 동시성 제어.
- 승인 과정에서의 스푸핑(Spoofing) 공격 방지를 위한 검증 로직 추가.
- 조회 API에 대해 `ADMIN` 인가(IDOR 차단) 로직을 추가하여 권한 밖의 데이터 접근 차단.
- API 이중 경로(e.g., `/api/api`) 오류 정정.
- JWT 및 CORS 환경변수 최적화 및 `ddl-auto=validate` 환경 호환성 확보.
- 감사 컬럼 (`created_by`, `updated_by`) 백필 마이그레이션 적용.

### 4. 카카오 로그인 연동 관련 큐(Queue) 처리
- 카카오 매칭 계정이 없는 경우, 이메일이 미인증/미제공이더라도 가입 승인 대기큐로 유입되도록 예외 처리 로직 수정.
- 카카오 신규 메일을 통한 회원가입 시 대기큐 유입, 그리고 매치 해제 시 다시 승인 대기큐로 복귀하는 흐름 구현.
